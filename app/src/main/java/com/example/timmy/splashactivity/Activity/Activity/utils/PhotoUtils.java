package com.example.timmy.splashactivity.Activity.Activity.utils;

import android.content.Intent;
import android.net.Uri;
import android.os.Environment;
import android.provider.MediaStore;
import android.view.View;

import java.io.File;

import static com.example.timmy.splashactivity.Activity.Activity.face.FaceUtil.REQUEST_CAMERA_IMAGE;

/**
 * Created by Timmy on 2017/9/24.
 */

public class PhotoUtils {
//    private void take_photo(View view) {
//        // 设置相机拍照后照片保存路径
//        mPictureFile = new File(Environment.getExternalStorageDirectory(),
//                "picture" + System.currentTimeMillis()/1000 + ".jpg");
//        // 启动拍照,并保存到临时文件
//        Intent mIntent = new Intent();
//        mIntent.setAction(MediaStore.ACTION_IMAGE_CAPTURE);
//        mIntent.putExtra(MediaStore.EXTRA_OUTPUT, Uri.fromFile(mPictureFile));
//        mIntent.putExtra(MediaStore.Images.Media.ORIENTATION, 0);
//        startActivityForResult(mIntent, REQUEST_CAMERA_IMAGE);
//    }
}
